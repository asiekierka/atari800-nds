name: Build

on:
  push:
    branches: [ port-nds-v2 ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container: skylyrac/blocksds:slim-latest
    steps:
      - name: Clone project
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install missing dependencies
        run: wf-pacman -Sy --noconfirm toolchain-gcc-arm-none-eabi-zlib

      - name: Build
        run: make -f Makefile.blocksds

      - name: Archive artifacts
        uses: actions/upload-artifact@v4
        with:
          name: atari800
          path: |
            atari800.nds
            COPYING

